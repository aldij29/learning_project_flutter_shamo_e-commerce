part of '../pages.dart';

class WhistlistPage extends StatelessWidget {
  const WhistlistPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Whistlist PAge'),
      ),
    );
  }
}